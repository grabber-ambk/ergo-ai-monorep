{
  "name": "@ergo-ai/features",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "echo 'Skipping build for features package'",
    "dev": "echo 'Skipping dev for features package'",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
  },
  "dependencies": {
    "react": "^18.2.0",
    "@ergo-ai/hooks": "workspace:*",
    "@ergo-ai/ui": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "@types/react": "^19.0.0"
  },
  "peerDependencies": {
    "react": "^18.0.0 || ^19.0.0"
  }
}
